version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
  build:
    commands:
      - echo Building...
      - npm install
      - npm run build
      - zip -r build.zip dist/*
artifacts:
  files:
    - './build.zip'